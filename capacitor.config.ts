import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.movies-app-dsklabs',
  appName: 'ionic-movies-app',
  webDir: 'www'
};

export default config;
