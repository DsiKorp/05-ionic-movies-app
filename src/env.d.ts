interface ImportMeta {
  readonly env: ImportMetaEnv;
}

interface ImportMetaEnv {
  /**
   * Agrega aquí tus variables con el prefijo NG_APP_
   * para tener autocompletado.
   */
  readonly NG_APP_API_KEY: string;
  // Agrega más si las necesitas
}